package dsaleedcode;
// package dsaLeedcode;

// public class Max_min_arrayValue_gfg {
    
// }
// import java.util.Arrays;
// class Compute 
// {
//     static pair getMinMax(long a[], long n)  
//     {
        //  long max = a[0];
        //  long min = a[0];
        //  for(int i = 1 ; i<n ; i++){
        //      if(a[i]<min){
        //          min = a[i];
        //      }
        //      else if(a[i]>max){
        //          max=a[i];
        //      }
        //  }
        //  pair p = new pair(min, max);
        //  return p ;
        // Array.sort(a);
        // long min = a[0];
        // long max =a[a.length-1];
        // return (newpair(min,max));
    // }
// }
